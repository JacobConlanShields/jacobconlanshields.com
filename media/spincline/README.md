# Spincline media

This folder is deployed as static media by Cloudflare Pages.

## Add files
- Place photo files in `photos/`.
- Place video files in `videos/`.

## Naming recommendation
Use lowercase names with hyphens to match the default gallery entries on `/pages/spincline/`.
